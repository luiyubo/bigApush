姓名: 收盘选股

在……之上：:
计划表：:
-cron：'107**1-5'cron: '10 7 * * 1-5'
工作流程分派(_D)：:

作业：:
运行选择：:
run-on:ubuntu-latest: Ubuntu-最新
timeout-minutes:60: 60
步骤：:
-使用：操作/结帐@v4uses: 操作/结帐@v4
-使用：操作/setup-python@v5uses: actions/setup-python@v5
和……一起：:
python版本：“3.11”: '3.11'
缓存：'皮普: 'Pip'
-运行：pip安装-R要求.txtrun: PIP安装-r要求.文本
-名称：还原数据库缓存名称: 还原数据库缓存
用法：操作/缓存@v4: 操作/缓存@v4
和……一起：:
路径：数据: 数据
密钥：stock-db-${{github.run_id}}: stock-db-${{github.run_id}}
restore-keys:stock-db-: stock-db-
-名称：检查数据库名称: 检查数据库
ID:check-db: check-db
运行：|: |
如果[-f"data/stock_selection.db"]；则
回声"exists=true">>$GITHUB_OUTPUT
其他
回声"exists=false">>$GITHUB_OUTPUT
Fi
-名称：初始化数据(首次运行)name: 初始化数据(首次运行)
if:steps.check-db.outputs.exists=='false': steps.check-db.outputs.exists=='false'
运行：Python main.py init: Python main.py init
timeout-minutes:45: 45
-python main.py运行
env:
    FEISHU_WEBHOOK：$server3_SEND_KEYsecrets。FEISHU_WEBHOOK30server3_SEND_KEYsecrets.FEISHU_WEBHOOK30}
    server3_SEND_KEY: ${{secrets.SERVER3_SEND_KEY}}  # 👈 加上这一行
运行：Python main.py运行运行: Python main.py运行
timeout-minutes:30 timeout-minutes: {
